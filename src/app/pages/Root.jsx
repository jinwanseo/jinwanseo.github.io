import styled from "styled-components";

const Title = styled.h1`
  font-size: 1.5em;
  text-align: center;
  color: teal;
`;

function Root(props) {
  return <Title>Install Styled Component!</Title>;
}

export default Root;
